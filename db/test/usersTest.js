describe('get all quiz results for users', function() {
  it.skip('should get all quiz data and answers for all users where quiz.public === true', function() {
    // should return active records for:
    // quizzes.id, quizzes.name
    // questions.id, questions.question
    // answers.id, answers.correct
    // user.id, user.email
  });
});

describe('checkIfUserExists', function() {
  it.skip('should return true if a user exists', function() {
  });

  it.skip('should return false if user does not exist', function() {
  });
});
